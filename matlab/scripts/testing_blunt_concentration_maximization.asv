k = 3;
N = 2*k;
H = -quadProgObjMatrix(k);
f = zeros(k,1);
A = []
b = []
Aeq = ones(k,1);
beq = 1;
alpha = quadprog(H,f,A,b,Aeq,beq)
concentration =  alpha' * H * alpha

function H = getQuadProgObjMatrix(k)
    [rowNum, colNum] = meshgrid(k:-1:1);
    H = 1./(2*rowNum+2*colNum+3);
end


